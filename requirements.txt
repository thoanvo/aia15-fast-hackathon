python-dotenv>=1.0.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
fastapi>=0.111.0
uvicorn>=0.30.0
streamlit>=1.36.0
requests>=2.31.0
httpx>=0.27.0
pydantic>=2.7.0

# LangChain-native AI/function-calling layer (src/langchain_app)
langchain-core>=0.3.0
langchain-classic>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0

# Vector store: FAISS (in-process, no external service, no onnxruntime)
faiss-cpu>=1.8.0
sentence-transformers>=3.0.0

# Out-of-scope detection (langchain_app/oos_guard.py) - cosine similarity
# over embeddings; already a transitive dependency of torch/sentence-transformers,
# listed explicitly since it's imported directly.
numpy>=1.26.0
# Local EN/VI language detection for oos_guard.detect_language() - avoids an
# extra LLM round-trip on the rejection path.
langdetect>=1.0.9

# SQL statement-count/type validation for the dynamic-SQL agent path
# (langchain_app/sql_validation.py) - lightweight, pure-Python tokenizer,
# no C extension.
sqlparse>=0.5.0

# Explicit state graph for the dynamic-SQL path's generate/validate/
# execute/retry loop (langchain_app/sql_graph.py). Pulls in ormsgpack (a
# compiled wheel) - verified no Windows DLL-load-order conflict with
# torch/sentence-transformers or SQLAlchemy in either import order.
langgraph>=0.6.0

# Chart-data rendering for the Streamlit frontend
# (frontend/components/response_display.py) - already a transitive
# dependency of streamlit, listed explicitly since it's imported directly.
pandas>=2.0.0

# Testing
pytest>=8.0.0

# Text-to-Speech (src/backend/services/tts_service.py)
# kokoro-onnx: lightweight Kokoro-82M ONNX TTS engine (CPU-only, ~122 MB model)
kokoro-onnx>=0.4.0
soundfile>=0.12.0
onnxruntime<1.21
